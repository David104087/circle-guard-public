---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: circleguard-do-stage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7.2
          ports:
            - containerPort: 6379
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: circleguard-do-stage
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
